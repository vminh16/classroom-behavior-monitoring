# deploy_bundle/models

This directory is intentionally excluded from Git.

The public-ready repository does not require duplicate model copies here. The deploy bundle is configured to reuse artifacts from `output_inference/`.

If you keep local mirrored copies in this folder for convenience, do not commit them.
