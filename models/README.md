# models

This directory is intentionally excluded from Git.

On the author's machine it contains legacy local copies of runtime models. It is not the canonical source of truth for this repository.

Use `output_inference/` instead. See [docs/ARTIFACTS.md](../docs/ARTIFACTS.md).
