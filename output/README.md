# output

This directory is intentionally excluded from Git.

It contains generated files only, for example:

- rendered demo videos
- benchmark logs
- tracker benchmark reports
- evaluation figures

Do not commit this directory to the public repository.
